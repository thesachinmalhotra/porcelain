import { createFileRoute } from "@tanstack/react-router"
import { useMemo, useState } from "react"
const components = [["Input","kafka"],["Input","http_server"],["Input","http_client"],["Input","file"],["Processor","mapping"],["Processor","jq"],["Processor","branch"],["Processor","workflow"],["Output","kafka"],["Output","http_client"],["Output","http_server"],["Output","stdout"]]
export const Route = createFileRoute("/components")({ component: Components })
function Components() { const [query,setQuery]=useState(""); const filtered=useMemo(()=>components.filter(([kind,name])=>name.includes(query.toLowerCase())||kind.toLowerCase().includes(query.toLowerCase())),[query]); return <div className="workspace-page"><header className="page-header"><div><span className="eyebrow">Manage</span><h1>Component catalog</h1><p>Common Redpanda Connect inputs, processors, and outputs available to author.</p></div></header><section className="panel"><div className="panel-header"><div><h2>Components</h2><p>{filtered.length} matching components</p></div><label className="input-search"><input aria-label="Search components" placeholder="Search components" value={query} onChange={e=>setQuery(e.target.value)} /></label></div><div className="catalog-grid">{filtered.map(([kind,name])=><article className="catalog-card" key={kind+name}><span>{kind}</span><strong>{name}</strong><small>Redpanda Connect</small></article>)}</div></section></div> }
